# InsureFlow_app
I designed and built InsureFlow, a microservices-based commercial insurance platform modeled on my current production application. The system enables users to generate quotes, bind them into policies, and automatically receive success notifications via their registered email addresses.
